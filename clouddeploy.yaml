steps:
  - name: 'gcr.io/cloud-builders/kubectl'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if [[ "$BRANCH_NAME" == "master" ]]; then
          kubectl apply -f deployment.yaml
        else
          echo "Skipping the command for branches other than master"
        fi
